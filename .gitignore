node_modules
dist
out
.DS_Store
*.db
*.db-shm
*.db-wal
.eslintcache
*.log*

# 로컬 테스트용 Electron userData (trash/notes/models 등 런타임 데이터 — 추적 안 함)
.rally-test-userdata/

# typescript incremental build cache (생성 산출물 — 추적 안 함)
*.tsbuildinfo

# coverage
coverage

# vitest blob reports (sharded coverage merge)
.vitest-reports

# env
.env
.env.*

# ai 
.claude
.bkit
.bkit-*
.bkit-*/**
docs