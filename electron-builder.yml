appId: com.jin.rally
productName: Rally
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
extraResources:
  - from: src/main/db/migrations
    to: migrations
  - from: dist-mcp
    to: dist-mcp
  - from: .claude/commands
    to: .claude/commands
  - from: .claude/skills
    to: .claude/skills
asarUnpack:
  - resources/**
  # sqlite-vec 네이티브 확장(.dylib/.so/.dll)은 asar 내부에서 로드 불가 → 언팩 필수
  - '**/node_modules/sqlite-vec/**'
  - '**/node_modules/sqlite-vec-*/**'
  # transformers.js + onnxruntime-node 네이티브 바이너리 → 언팩 필수
  - '**/node_modules/@xenova/transformers/**'
  - '**/node_modules/onnxruntime-node/**'
win:
  executableName: rally
  # 1차 Windows 배포는 x64 단일. Apple Silicon에서 bare `electron-builder --win` 실행 시
  # 기본 arm64 산출물이 생기는 혼동 방지. arm64 추가 시 artifactName에 ${arch} 필요.
  target:
    - target: nsis
      arch:
        - x64
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
mac:
  category: public.app-category.productivity
  hardenedRuntime: true
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  notarize: true
  extendInfo:
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
dmg:
  artifactName: ${name}.${ext}
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: false
publish:
  provider: github
  owner: warizi
  repo: Rally
  releaseType: draft
