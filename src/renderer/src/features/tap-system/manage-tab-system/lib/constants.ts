// 패인 기본 설정
export const PANE_DEFAULTS = {
  MIN_SIZE: 200,
  DEFAULT_SIZE: 50
} as const

// 레이아웃 기본 설정
export const LAYOUT_DEFAULTS = {
  DEFAULT_PANE_ID: 'main',
  DEFAULT_SPLIT_SIZE: 50
} as const
